# App Templates

## Prepare App Template

Currently only `tar.gz` format is supported.
Navigate to the folder containing the application package and run the following command to compress the application:

```bash
tar czf -C <app-folder> <template-name>.tar.gz .
```

## Upload App Template

